import { Button, styled } from "@mui/material";
export const AuthButton = styled(Button)({
    color: '#FFFFFF',
    backgroundColor: '#5570F1',
    padding: 8,
    borderRadius: 4,
    '&:hover':{
      backgroundColor: '#3F5CEA',
    }
  }
);
 
